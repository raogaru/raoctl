-- ----------------------------------------------------------------------
-- wf.ini
-- ----------------------------------------------------------------------

PROMPT Initial Data ...

INSERT INTO WF_PARTICIPANTS ( ID, NAME, DESCRIPTION, PARTICIPANT_TYPE, REPLICATION_TIMESTAMP ) VALUES ( 1, 'PL/FLOW', 'The workflow engine itself', 'SYSTEM', NULL);

COMMIT;

-- ----------------------------------------------------------------------
-- wf.ini
-- ----------------------------------------------------------------------
